diff --git a/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Queries.m b/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Queries.m
index d6d61ef..e7fc322 100644
--- a/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Queries.m
+++ b/node_modules/react-native-health/RCTAppleHealthKit/RCTAppleHealthKit+Queries.m
@@ -254,17 +254,18 @@ - (void)fetchQuantitySamplesOfType:(HKQuantityType *)quantityType
             dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, 0), ^{
 
                 for (HKQuantitySample *sample in results) {
+                    @try {
                     HKQuantity *quantity = sample.quantity;
                     double value = [quantity doubleValueForUnit:unit];
 
-                    NSString *startDateString = [RCTAppleHealthKit buildISO8601StringFromDate:sample.startDate];
-                    NSString *endDateString = [RCTAppleHealthKit buildISO8601StringFromDate:sample.endDate];
+                    NSString *startDateString = [RCTAppleHealthKit buildISO8601StringFromDate:sample.startDate] ?: @"";
+                    NSString *endDateString = [RCTAppleHealthKit buildISO8601StringFromDate:sample.endDate] ?: @"";
 
                     NSMutableDictionary *elem = [NSMutableDictionary dictionaryWithDictionary:@{
                             @"value" : @(value),
-                            @"id" : [[sample UUID] UUIDString],
-                            @"sourceName" : [[[sample sourceRevision] source] name],
-                            @"sourceId" : [[[sample sourceRevision] source] bundleIdentifier],
+                            @"id" : [[sample UUID] UUIDString] ?: @"",
+                            @"sourceName" : [[[sample sourceRevision] source] name] ?: @"",
+                            @"sourceId" : [[[sample sourceRevision] source] bundleIdentifier] ?: @"",
                             @"startDate" : startDateString,
                             @"endDate" : endDateString,
                     }];
@@ -275,6 +276,9 @@ - (void)fetchQuantitySamplesOfType:(HKQuantityType *)quantityType
                     }
 
                     [data addObject:elem];
+                    } @catch (NSException *exception) {
+                        NSLog(@"RNHealth: error processing quantity sample: %@", exception);
+                    }
                 }
 
                 completion(data, error);
@@ -604,10 +608,11 @@ - (void)fetchSleepCategorySamplesForPredicate:(NSPredicate *)predicate
             dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, 0), ^{
 
                 for (HKCategorySample *sample in results) {
+                    @try {
                     NSInteger val = sample.value;
 
-                    NSString *startDateString = [RCTAppleHealthKit buildISO8601StringFromDate:sample.startDate];
-                    NSString *endDateString = [RCTAppleHealthKit buildISO8601StringFromDate:sample.endDate];
+                    NSString *startDateString = [RCTAppleHealthKit buildISO8601StringFromDate:sample.startDate] ?: @"";
+                    NSString *endDateString = [RCTAppleHealthKit buildISO8601StringFromDate:sample.endDate] ?: @"";
 
                     NSString *valueString;
 
@@ -639,15 +644,18 @@ - (void)fetchSleepCategorySamplesForPredicate:(NSPredicate *)predicate
                   }
 
                     NSDictionary *elem = @{
-                            @"id" : [[sample UUID] UUIDString],
+                            @"id" : [[sample UUID] UUIDString] ?: @"",
                             @"value" : valueString,
                             @"startDate" : startDateString,
                             @"endDate" : endDateString,
-                            @"sourceName" : [[[sample sourceRevision] source] name],
-                            @"sourceId" : [[[sample sourceRevision] source] bundleIdentifier],
+                            @"sourceName" : [[[sample sourceRevision] source] name] ?: @"",
+                            @"sourceId" : [[[sample sourceRevision] source] bundleIdentifier] ?: @"",
                     };
 
                     [data addObject:elem];
+                    } @catch (NSException *exception) {
+                        NSLog(@"RNHealth: error processing sleep sample: %@", exception);
+                    }
                 }
 
                 completion(data, error);
